<h1>Liste des articles récents</h1>

<% @posts.each do |post| %>
    <div>
        <h3><%= post.title %></h3>
        <p><%= post.content %></p>
        <%= link_to 'Consulter', post_path(post.id) %>
    </div>

<% end %>
